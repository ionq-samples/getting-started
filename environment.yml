name: myenv
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - matplotlib
  - jupyter
  # Add other dependencies here, for Conda packages.
  - pip:
      # Pip-only packages here, if any.
      - qiskit
      - qiskit-ionq
      - cirq
      - cirq-ionq
      - pennylane
      - pennylane-ionq
      - projectq
      - cudaq
      - pylatexenc
      - requests
