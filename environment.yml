name: myenv
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12.3
  - pip
  - matplotlib
  - jupyter
  # Add other dependencies here, for Conda packages.
  - pip:
      # Pip-only packages here, if any.
      - qiskit
      - qiskit-ionq
      - cirq
      - cirq-ionq
      - pennylane
      - pennylane-ionq
      - projectq
      - qbraid[runtime]
      - cuda-quantum
      - pylatexenc
      - requests
